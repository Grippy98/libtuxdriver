git remote add grippy https://github.com/Grippy98/libtuxdriver
git checkout -b dev-monkeying
git add .
git commit -m "Add voice control, wake word, and context-aware animations"
git push -u grippy dev-monkeying
